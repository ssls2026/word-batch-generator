<Window x:Class="WordBatchGenerator.Gui.PdfViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="PDF 预览" Height="760" Width="900"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Border Background="#F7F4EF" 
            BorderBrush="#E7E1D7" 
            BorderThickness="1.5" 
            CornerRadius="12" 
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.15" Color="#000000"/>
        </Border.Effect>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 顶部操作栏 -->
            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <Button DockPanel.Dock="Right" Content="✕" Width="28" Height="28" Background="Transparent" BorderThickness="0" Foreground="#5C635D" FontSize="14" Cursor="Hand" Click="BtnClose_Click" Margin="12,0,0,0"/>
                <Button DockPanel.Dock="Right" Content="🖨️ 打印" Width="80" Height="28" Padding="0" Style="{StaticResource PrimaryButton}" Click="BtnPrint_Click" VerticalAlignment="Center"/>
                <TextBlock x:Name="TxtTitle" Text="📄 PDF 预览 - 合并文档.pdf" FontSize="14.5" FontWeight="Bold" Foreground="#1F2421" VerticalAlignment="Center"/>
            </DockPanel>

            <!-- WebView2 容器 -->
            <Border Grid.Row="1" Background="White" BorderBrush="#EBE7DE" BorderThickness="1.5" CornerRadius="8" ClipToBounds="True">
                <Grid>
                    <!-- 加载中提示 -->
                    <StackPanel x:Name="PanelLoading" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="正在初始化 PDF 渲染引擎..." FontSize="13" Foreground="#8A8070" Margin="0,0,0,10"/>
                        <ProgressBar Height="4" Width="180" IsIndeterminate="True" Foreground="#7A9BA3" Background="#EBE7DE" BorderThickness="0"/>
                    </StackPanel>
                    
                    <wv2:WebView2 x:Name="WebView" Visibility="Hidden"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
