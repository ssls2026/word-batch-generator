<Window x:Class="WordBatchGenerator.Gui.PrintWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="打印设置" Height="340" Width="440"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Border Background="#F7F4EF" 
            BorderBrush="#E7E1D7" 
            BorderThickness="1.5" 
            CornerRadius="12" 
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.15" Color="#000000"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <DockPanel Grid.Row="0" Margin="0,0,0,16">
                <Button DockPanel.Dock="Right" Content="✕" Width="28" Height="28" Background="Transparent" BorderThickness="0" Foreground="#5C635D" FontSize="14" Cursor="Hand" Click="BtnClose_Click"/>
                <TextBlock Text="🖨️ 快速打印设置" FontSize="15.5" FontWeight="Bold" Foreground="#1F2421" VerticalAlignment="Center"/>
            </DockPanel>

            <!-- 设置区域 -->
            <StackPanel Grid.Row="1">
                <!-- 打印机选择 -->
                <TextBlock Text="选择打印机" FontSize="13" FontWeight="SemiBold" Foreground="#5C635D" Margin="0,0,0,8"/>
                <ComboBox x:Name="CmbPrinters" Height="38" Margin="0,0,0,20" FontSize="13" VerticalContentAlignment="Center" Background="White" BorderBrush="#E7E1D7" BorderThickness="1.5"/>

                <!-- 单双面选择 -->
                <TextBlock Text="单双面设置" FontSize="13" FontWeight="SemiBold" Foreground="#5C635D" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <RadioButton x:Name="RadOneSided" Content="单面打印" IsChecked="True" Margin="0,0,16,0" FontSize="13" Foreground="#1F2421" VerticalContentAlignment="Center"/>
                    <RadioButton x:Name="RadDuplexLong" Content="双面长边翻页" Margin="0,0,16,0" FontSize="13" Foreground="#1F2421" VerticalContentAlignment="Center"/>
                    <RadioButton x:Name="RadDuplexShort" Content="双面短边翻页" FontSize="13" Foreground="#1F2421" VerticalContentAlignment="Center"/>
                </StackPanel>
            </StackPanel>

            <!-- 底部操作 -->
            <DockPanel Grid.Row="2" Margin="0,12,0,0">
                <CheckBox x:Name="ChkSaveDefault" Content="记住打印设置" FontSize="13" Foreground="#5C635D" VerticalContentAlignment="Center" VerticalAlignment="Center"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Width="80" Height="38" Padding="0" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="BtnClose_Click"/>
                    <Button Content="确认打印" Width="100" Height="38" Padding="0" Style="{StaticResource PrimaryButton}" Click="BtnPrint_Click"/>
                </StackPanel>
            </DockPanel>
        </Grid>
    </Border>
</Window>
