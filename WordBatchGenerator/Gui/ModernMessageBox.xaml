<Window x:Class="WordBatchGenerator.Gui.ModernMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="提示" Width="400" MinHeight="180"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        SizeToContent="Height">
    
    <Border Background="#F7F4EF" 
            BorderBrush="#E7E1D7" 
            BorderThickness="1.5" 
            CornerRadius="12" 
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.15" Color="#000000"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <Button DockPanel.Dock="Right" Content="✕" Width="24" Height="24" Background="Transparent" BorderThickness="0" Foreground="#5C635D" FontSize="12" Cursor="Hand" Click="BtnCancel_Click"/>
                <TextBlock x:Name="TxtTitle" Text="提示" FontSize="14" FontWeight="Bold" Foreground="#1F2421" VerticalAlignment="Center"/>
            </DockPanel>

            <!-- 内容区域 -->
            <Grid Grid.Row="1" Margin="0,4,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="TxtIcon" Text="💡" FontSize="26" VerticalAlignment="Top" Margin="0,0,12,0"/>
                
                <TextBlock Grid.Column="1" x:Name="TxtMessage" Text="消息内容" FontSize="13.5" Foreground="#2C322E" TextWrapping="Wrap" VerticalAlignment="Center" LineHeight="18"/>
            </Grid>

            <!-- 底部按钮 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnSecondary" Content="取消" Width="80" Height="36" Padding="0" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Visibility="Collapsed" Click="BtnSecondary_Click"/>
                <Button x:Name="BtnPrimary" Content="确定" Width="90" Height="36" Padding="0" Style="{StaticResource PrimaryButton}" Click="BtnPrimary_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
